import { Link } from 'lucide-react';

const Header = () => {
  return (
    <header className="justify-beetween flex">
      <nav>
        <Link />
      </nav>
      {/* 다크, 라이트 토글 */}
    </header>
  );
};

export default Header;
